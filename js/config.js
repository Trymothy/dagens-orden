// ============================================================
//  DAGENS ORDEN — Site Configuration
// ============================================================

window.DO_CONFIG = {
  // GitHub source for articles
  githubOwner: 'Trymothy',
  githubRepo: 'dagens-orden',
  branch: 'main',
  dataFile: 'data/articles.json',

  // Contact form endpoint (Formspree)
  // Get yours at https://formspree.io → New Form → connect to redaksjon@dagensorden.no
  // Then paste the form ID below (just the ID, e.g. "xyznnnnn")
  formspreeId: 'REPLACE_WITH_YOUR_FORMSPREE_ID',

  // Editorial email (shown as fallback mailto link)
  editorEmail: 'redaksjon@dagensorden.no'
};
